# RAG-1
A Retrieval-Augmented Generation (RAG) application built using LangChain, ChromaDB, HuggingFace Embeddings, and Ollama. The system loads PDF documents, splits them into chunks, generates vector embeddings, stores them in ChromaDB, and retrieves relevant context to answer user queries using a local LLM.
